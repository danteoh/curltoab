{
  "name": "curltoab",
  "version": "1.0.0",
  "description": "An online tool to convert a curl request to an apache bench command",
  "main": "index.js",
  "scripts": {
    "build": "browserify app.js -o dist/app.js -t [ babelify --presets [ es2015 react ] ]",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Teoh",
  "license": "ISC",
  "dependencies": {
    "react": "^15.1.0",
    "react-dom": "^15.1.0"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.3.0",
    "browserify": "^13.0.1"
  }
}
